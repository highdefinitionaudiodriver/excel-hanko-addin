{
  "name": "excel-hanko-addin",
  "version": "1.0.0",
  "description": "Japanese electronic stamp (Hanko/Data-in) add-in for Excel",
  "scripts": {
    "start": "npx office-addin-dev-certs install && npx http-server src -p 3000 -S -C %USERPROFILE%/.office-addin-dev-certs/localhost.crt -K %USERPROFILE%/.office-addin-dev-certs/localhost.key --cors",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "office-addin-dev-certs": "^2.0.6"
  }
}
